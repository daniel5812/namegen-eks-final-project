apiVersion: v1
kind: Service
metadata:
  name: namegen-service
spec:
  type: NodePort
  selector:
    app: namegen
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080