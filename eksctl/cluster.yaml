apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: namegen-eks-cluster
  region: eu-central-1
  version: "1.32"

autoModeConfig:
  enabled: true
  nodePools:
    - general-purpose
    - system

iam:
  withOIDC: true
